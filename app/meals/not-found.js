export default function NotFound(){
    return <main className="not-found">
        <h1>Meal not found</h1>
        <p>Unfortunately, we could not find the requested page meal or data</p>
    </main>
}